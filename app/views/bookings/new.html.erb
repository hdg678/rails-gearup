<h1>It works</h1>

 <%= simple_form_for [:equipment, @booking] do |f| %>
  <%= f.input :pickup %>
  <%= f.input :dropoff %>
  <%= f.button :submit, class: "btn btn-primary" %>
  <%= link_to "Cancel", equipment_index_path, class: "btn btn-info"%>
  <% end %>
